package email

import (
	"testing"
)

func TestClientConnection(t *testing.T) {
	// user := "mail@mail.net"
	// password := "secret"
	// if err := Send(user, password); err != nil {
	// 	t.Fatal(err)
	// }
	// smtp_host := "smtp.yandex.ru"
	// smtp_port := 587

}
